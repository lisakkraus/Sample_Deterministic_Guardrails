# Treat image files as binary to prevent corruption and ensure they are downloadable
*.png binary
*.jpg binary
*.jpeg binary

# Alternative explicit unsetting (optional but very clear)
# *.png -text -diff

# Force the GIF to be treated as a text file for the scraper
Secret_Code.txt.gif text